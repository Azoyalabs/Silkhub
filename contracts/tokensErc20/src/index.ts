
import {}

export {

}


/*
type MintRecipient = {
    beneficiary: `0x{string}`,
    amount: bigint
}

type Metadata = {
    name: string,
    symbol: string,
    
    // decimals not directly settable in the constructor for openzeppelin? 
    // is uint8, set max at 18 decimals in instantiation if we do want to modify this 
    decimals: number
}

type TokenConfig = {
    isMintable: boolean,
    isBurnable: boolean,

    metadata: Metadata,

    // only supply minter? 
    initialSupply: bigint,
}
*/