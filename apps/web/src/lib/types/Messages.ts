/**
 * Form return Messages
 */

export type TokenCreationMessage = {
	address: string;
};

export type AirdropCreationMessage = {
	address: string;
};

export type SaleCreationMessage = {
	address: string;
	token: string;
}